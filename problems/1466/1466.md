# 1466. Reorder Routes to Make All Paths Lead to the City Zero

## 题目链接

- https://leetcode.com/problems/reorder-routes-to-make-all-paths-lead-to-the-city-zero/

## 题目要求

给定 `n`, `connections`，完成 `Reorder Routes to Make All Paths Lead to the City Zero` 对应的 `minReorder` 逻辑，并返回题目要求的结果。

## 示例

### 示例 1

- 输入：`n = 6, connections = [[0, 1], [1, 3], [2, 3], [4, 0], [4, 5]]`
- 输出：`3`
- 解释：本地样例用于验证你的实现是否符合题目核心规则。

## 约束条件

- `n` 必须按原题含义处理。
- `connections` 必须按原题含义处理。
- 注意空输入、重复值、边界下标和结果顺序等边界情况。

## 解题思路

- 先把输入关系建成邻接表或状态图。
- 根据是否有方向、入度或层次选择 DFS/BFS/拓扑。

## 推荐题型

- 主模式：`图 / BFS / DFS / 拓扑排序`

## 提示

- 写代码前先定义：图中的点和边分别表示什么。
- 判断关键状态是遍历顺序、入度，还是访问标记。

## 复杂度目标

- 图遍历类题目的常见目标是 O(V + E)。

## 本地练习清单

- 先完整读一遍原题。
- 再用自己的中文把题意复述一遍。
- 在 `1466_ReorderRoutestoMakeAllPathsLeadtotheCityZero.cpp` 的待实现区域补全算法。
- 运行本地样例，对照“当前结果”和“预期结果”。
