# 25. Reverse Nodes in k-Group

## 题目链接

- https://leetcode.com/problems/reverse-nodes-in-k-group/

## 题目要求

给定 `head`, `k`，完成 `Reverse Nodes in k-Group` 对应的 `reverseKGroup` 逻辑，并返回题目要求的结果。

## 示例

### 示例 1

- 输入：`lists = [[1, 2, 3, 4, 5]], args = [2]`
- 输出：`[2, 1, 4, 3, 5]`
- 解释：本地样例用于验证你的实现是否符合题目核心规则。

## 约束条件

- `head` 必须按原题含义处理。
- `k` 必须按原题含义处理。
- 注意空输入、重复值、边界下标和结果顺序等边界情况。

## 解题思路

- 先画出指针变化过程。
- 改 next 指针前保存后续节点，必要时使用 dummy。

## 推荐题型

- 主模式：`链表`

## 提示

- 如果链表结构会变化，建议先把指针移动过程画出来。
- 每次修改前后，都要明确每个指针到底指向哪里。

## 复杂度目标

- 根据指针策略不同，通常目标是一遍或两遍扫描完成。

## 本地练习清单

- 先完整读一遍原题。
- 再用自己的中文把题意复述一遍。
- 在 `025_ReverseNodesinkGroup.cpp` 的待实现区域补全算法。
- 运行本地样例，对照“当前结果”和“预期结果”。
